"""zticli — the `zti` client CLI (ADR-0004, opened under ADR-0009).

Distinct from the open runtime's `ztip` console script: `ztip` is the envelope
runtime for the open protocol; `zti` is the client gate layer (this package),
which reports to a ZTI Core plane."""
